# Install platform-specific binaries for both host (macOS) and devcontainer (Linux).
# Prevents esbuild/other native packages from failing when node_modules
# are shared between host and container via Docker volume mounts.
supportedArchitectures[os][]=linux
supportedArchitectures[os][]=darwin
supportedArchitectures[cpu][]=arm64
supportedArchitectures[cpu][]=x64
