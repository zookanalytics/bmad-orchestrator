---
"@zookanalytics/agent-env": minor
---

Add version drift detection and auto-restart for the interactive menu, preventing opaque CONFIG_REFRESH_FAILED errors when agent-env is upgraded while the menu is running
