---
"@zookanalytics/agent-env": minor
---

Add devcontainer config merge pipeline that replaces the binary baseline/repo choice with a deep merge strategy, always combining managed properties with repo config
