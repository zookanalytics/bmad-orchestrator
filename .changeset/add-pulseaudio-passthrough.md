---
"@zookanalytics/agent-env": minor
---

Add PulseAudio audio passthrough for Claude voice mode in containers. New `setup-audio` CLI command configures macOS PulseAudio with TCP module and cookie-based auth, enabling ALSA audio routing from containers to the host.
