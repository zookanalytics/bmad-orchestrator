---
"@zookanalytics/bmm-retrospective-module": minor
---

Add bin entry for npx-based installation. Consumers can install workflows via `npx @zookanalytics/bmm-retrospective-module` or target a specific project with `--directory`.
